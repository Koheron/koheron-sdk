---
instrument: peak_detector
board: red-pitaya

config_registers:
  - fpga/cores/peak_address_low
  - fpga/cores/peak_address_high
  - fpga/cores/peak_address_reset

status_registers:
  - peak_address
  - peak_maximum

cores:
  - comparator_v1_0

parameters:
  wfm_width: 8

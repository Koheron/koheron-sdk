/// Hardware addresses
///
/// (c) Koheron

#ifndef __DRIVERS_CORE_ADDRESSES_HPP__
#define __DRIVERS_CORE_ADDRESSES_HPP__

// -- Base addresses
{% for addr in dic['addresses'] -%}
#define {{ addr|upper }}_ADDR {{ dic['addresses'][addr] }}
{% endfor %}

// -- Config offsets
{% for offset in dic['config_offsets'] -%}
#define {{ offset|upper }}_OFF {{ 4 * dic['config_offsets'][offset] }}
{% endfor %}
// -- Status offsets
{% for offset in dic['status_offsets'] -%}
#define {{ offset|upper }}_OFF {{ 4 * dic['status_offsets'][offset] }}
{% endfor %}
#endif // __DRIVERS_CORE_ADDRESSES_HPP__

#define BITSTREAM_ID_OFF 0

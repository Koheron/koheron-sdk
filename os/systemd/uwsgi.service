[Unit]
Description=uWSGI
After=network.target

[Service]
ExecStartPre=/bin/mkdir -p /var/run/uwsgi
ExecStartPre=/bin/mkdir -p /var/log/uwsgi
ExecStart=/usr/bin/uwsgi --ini /etc/uwsgi/uwsgi.ini --plugin python3
Restart=always
KillSignal=SIGKILL
Type=notify
NotifyAccess=all

[Install]
WantedBy=multi-user.target

machine:
  services:
    - docker
dependencies:
  override:
    - docker info
    - docker build -t koheron/koheron-sdk .
test:
  pre:
    - docker run --env "DOCKER=True" koheron/koheron-sdk  bash scripts/tests/ci_tests.sh

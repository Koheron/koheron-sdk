version: 2

jobs:
  build:
    docker:
      - image: circleci/python:2.7-stretch-browsers
      - image: circleci/node:10.11.0-stretch-browsers
    steps:
      - checkout
      - run:
          name: Append dev to source.list
          command: sudo bash -c "echo deb http://archive.ubuntu.com/ubuntu/ xenial main >> /etc/apt/sources.list"; sudo bash -c "echo deb-src http://archive.ubuntu.com/ubuntu/ xenial main >> /etc/apt/sources.list"
      - run:
          name: Install GCC
          command: sudo apt-get update; sudo apt-get install -y --allow-unauthenticated gcc-5 g++-5 gcc-5-arm-linux-gnueabihf g++-5-arm-linux-gnueabihf; sudo update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-5 100; sudo update-alternatives --install /usr/bin/g++ g++ /usr/bin/g++-5 100
      - run:
          name: Link GCC
          command: sudo ln -s /usr/bin/arm-linux-gnueabihf-gcc-5 /usr/bin/arm-linux-gnueabihf-gcc; sudo ln -s /usr/bin/arm-linux-gnueabihf-gcc-5 /usr/bin/arm-linux-gnueabihf-g++
      - run:
          name: Install python requirements
          command: sudo pip install -r requirements.txt
      - run:
          name: Install Koheron python
          command: sudo pip install python/.
      - run:
          name: install twine
          command: sudo pip install twine
      - run:
          name: Setup web
          command: curl -sL https://deb.nodesource.com/setup_10.x | sudo -E bash -; sudo make setup_web
      - run:
          name: Build server
          command: bash build_examples.sh server
      - run:
          name: Build web
          command: bash build_examples.sh web
  deploy:
    machine:
      enabled: true
    steps:
      - run:
          name: Upload Koheron python to pypi
          command: make PYPI_USERNAME=$PYPI_USERNAME PYPI_PASSWORD=$PYPI_PASSWORD upload_pip

workflows:
  version: 2
  build-deplay:
    jobs:
      - build
      - deploy:
          filters:
            branches:
              only: master

# # https://discuss.circleci.com/t/caching-apt-get-install-results/563/3
# dependencies:
#   pre:
#     - sudo bash -c "echo deb http://archive.ubuntu.com/ubuntu/ xenial main >> /etc/apt/sources.list"
#     - sudo bash -c "echo deb-src http://archive.ubuntu.com/ubuntu/ xenial main >> /etc/apt/sources.list"
#     - sudo apt-get update; sudo apt-get install gcc-5 g++-5 gcc-5-arm-linux-gnueabihf g++-5-arm-linux-gnueabihf
#     - sudo update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-5 100
#     - sudo update-alternatives --install /usr/bin/g++ g++ /usr/bin/g++-5 100
#     - sudo ln -s /usr/bin/arm-linux-gnueabihf-gcc-5 /usr/bin/arm-linux-gnueabihf-gcc
#     - sudo ln -s /usr/bin/arm-linux-gnueabihf-g++-5 /usr/bin/arm-linux-gnueabihf-g++
#     - pip install -r requirements.txt
#     - pip install python/.
#     - npm install -g typescript
#     - npm install @types/jquery@2.0.46 @types/jquery-mousewheel@3.1.5 websocket @types/node
#     - pip install twine

# test:
#   pre:
#     - bash build_examples.sh server
#     - bash build_examples.sh web

# deployment:
#   hub:
#     branch: master
#     commands:
#       - make PYPI_USERNAME=$PYPI_USERNAME PYPI_PASSWORD=$PYPI_PASSWORD upload_pip
